var input = require('fs').readFileSync('/dev/stdin', 'utf8');
var lines = input.split('\n');
var input = require('fs').readFileSync('/dev/stdin', 'utf8');
var lines = input.split('\n');

let s = 0;
let b = 2;

for (let a = 3; a <= 39; a = a + 2) {
  s += a/b;
  b *= 2;
}

let calculoFinal = s + 1;
console.log(calculoFinal.toFixed(2));
