var input = require('fs').readFileSync('/dev/stdin', 'utf8');
var lines = input.split('\n');
var prompt = function(texto) { return lines.shift();};

    var tempoGasto= parseInt(prompt("Qual a distancia entre os carros?"))
    var velocidadeMedia= parseInt(prompt("Velocidade media de veiculo em Km/H:"))
    var distancia= parseFloat
    console.log(((distancia= velocidadeMedia*tempoGasto)/12).toFixed(3))
